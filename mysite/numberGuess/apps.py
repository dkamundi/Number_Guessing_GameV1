from django.apps import AppConfig

# Define a configuration class for the 'numberGuess' app.
class NumberguessConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'numberGuess'
